name: flutter_traffic_light
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  collection:
  cupertino_icons:
  flutter:
    sdk: flutter
  flutter_riverpod:
  flutter_web_plugins:
    sdk: flutter
  freezed_annotation:
  gap:
  riverpod_annotation:
  roggle:

dev_dependencies:
  build_runner:
  custom_lint:
  flutter_gen_runner:
  flutter_test:
    sdk: flutter
  freezed:
  pedantic_mono:
  riverpod_generator:
  riverpod_lint:

flutter:
  uses-material-design: true
